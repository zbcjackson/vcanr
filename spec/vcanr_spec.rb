# frozen_string_literal: true

RSpec.describe Vcanr do
  it "has a version number" do
    expect(Vcanr::VERSION).to be "0.1.0"
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
